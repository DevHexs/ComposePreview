<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposePreview"
        tools:targetApi="31" >
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.ComposePreview" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TasksActivity"
            android:exported="false"
            android:label="@string/title_activity_tasks"
            android:theme="@style/Theme.ComposePreview" >
        </activity>
        <activity
            android:name=".BlackHoleActivity"
            android:exported="false"
            android:label="@string/title_activity_black_hole"
            android:theme="@style/Theme.ComposePreview" />
        <activity
            android:name=".HappyBirthdayActivity"
            android:exported="false"
            android:label="@string/title_activity_happy_birthday"
            android:theme="@style/Theme.ComposePreview" />
        <activity
            android:name=".ComposeDialActivity"
            android:exported="false"
            android:label="@string/title_activity_compose_dial"
            android:theme="@style/Theme.ComposePreview" />

    </application>

</manifest>